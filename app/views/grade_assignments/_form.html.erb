<%= form_with(model: grade_assignment) do |form| %>
  <% if grade_assignment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(grade_assignment.errors.count, "error") %> prohibited this grade_assignment from being saved:</h2>

      <ul>
        <% grade_assignment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%= form.text_field :student_id %>
  </div>

  <div>
    <%= form.label :grade_id, style: "display: block" %>
    <%= form.text_field :grade_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
